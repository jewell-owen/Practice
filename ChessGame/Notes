To-DO

1). King movePiece JUnit tests
2). Check JUnit tests
3). CheckMate JUnit tests

4). Implement UI
5). Test UI



02/09/24 Notes
	-Switch to grid layout
	-Build full screen of piece buttons to test/reuse code
	
	-Possibly reorganize images into folders first
	(This way don't have to go back and add file path where image locations are referenced)